#!/bin/bash

# add plugins in .zshrc before oh my zsh is initialized echo 'plugins=(git)' >> ~/.zshrc
# plugins=(git docker asdf dotenv docker gh golang terraform macos npm podman ripgrep thefuck tmux ssh rust)

export EDITOR="nvim"
export VISUAL="nvim"
export LANG="en_US.UTF-8"

export XDG_CONFIG_HOME="$HOME/.config"
export ZDOTDIR="$HOME/"

export ZSH_AUTOSUGGEST_STRATEGY=(completion history)

# macOS specifics
if [[ $(uname) == "Darwin" ]]; then
	. "/opt/homebrew/opt/asdf/libexec/asdf.sh"

	source "$(brew --prefix)/share/zsh-autosuggestions/zsh-autosuggestions.zsh"

	export BROWSER="arc"

	# Temp disable, nightly was a bit unreliable. Use nvim from brew
	# export PATH="$HOME/prog/nvim-macos-arm64/bin:$PATH"
	# export PATH="$HOME/prog/thirdparty/nvim-macos-x86_64/bin:$PATH"
fi

alias reloadall="yabai --restart-service && skhd --restart-service && sketchybar --reload"

alias la="ls -lah"
alias vi="nvim"
alias vim="nvim"

alias tmls="tmux ls"
alias tma="tmux attach"

alias lg="lazygit"

alias gaac="git add -A && git commit --amend --no-edit"

alias f='cd $(fd --type directory | fzf)'
eval "$(fzf --zsh)"

eval "$(thefuck --alias)"
eval "$(zoxide init zsh)"

cd "$HOME" || exit
